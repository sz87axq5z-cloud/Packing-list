# Copy this file to .env and fill in values
# Google OAuth credentials from Google Cloud Console (OAuth 2.0 Client ID of type Web application)
# IMPORTANT: Do NOT commit real credentials. Use placeholders in this example file.
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Session secret used to sign cookies (use a long random string in production)
SESSION_SECRET=change-me-to-a-long-random-string

# Optional: override if needed
# SECRET_KEY=

# Optional: Set a fixed base URL for OAuth redirect to avoid redirect_uri mismatches.
# Example values:
#   OAUTH_REDIRECT_BASE_URL=http://127.0.0.1:8000
#   OAUTH_REDIRECT_BASE_URL=http://localhost:8000
#   OAUTH_REDIRECT_BASE_URL=https://your-production-domain.com
# If set, the app will use "{OAUTH_REDIRECT_BASE_URL}/auth/callback" as the redirect_uri.
OAUTH_REDIRECT_BASE_URL=

# Optional: where to redirect users after successful Google login
# Defaults to "/student/" which serves the local docs/ directory.
# Example values:
#   STUDENT_URL=/student/
#   STUDENT_URL=https://your-domain.com/student/
STUDENT_URL=
